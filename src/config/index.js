import { auth, database, storage, uiConfig } from "./firebaseConfig";
import { AuthProvider, AuthContext } from "./AuthContext";
export { auth, database, storage, uiConfig, AuthProvider, AuthContext };
